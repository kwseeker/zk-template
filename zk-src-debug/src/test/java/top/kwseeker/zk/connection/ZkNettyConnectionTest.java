package top.kwseeker.zk.connection;

/**
 * NettyServerCnxnFactory$start()
 *  parentChannel = bootstrap.bind(localAddress).syncUninterruptibly().channel();
 */
public class ZkNettyConnectionTest {
}
